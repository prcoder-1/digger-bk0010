#pragma once
#include <stdint.h>

// Спрайты полного шрифта 12x12 пикселей

extern const uint8_t ch_digits[10][12][3]; // Символы цифр ('0'-'9')
extern const uint8_t ch_alpha[26][12][3];  // Символs алфавита ('A'-'Z')
extern const uint8_t ch_dot[12][3];        // Символ '.' (точка)
extern const uint8_t ch_underline[12][3];  // Символ '_' (подчёркивание)
extern const uint8_t ch_space[12][3];      // Символ ' ' (пробел)
